<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wit.cowcalendar">

    <application
        android:name="org.wit.cowcalendar.main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">

        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.login.LoginView"
            android:label="@string/app_name"
            android:launchMode="singleTop">
        </activity>


        <activity android:name="org.wit.cowcalendar.activities.AnimalView">
        </activity>

        <activity android:name="org.wit.cowcalendar.views.animalList.AnimalListView">
        </activity>

        <activity android:name="org.wit.cowcalendar.activities.AnimalEventView">
        </activity>

        <activity android:name="org.wit.cowcalendar.views.addServe.AddServeView">
        </activity>

        <activity android:name="org.wit.cowcalendar.views.addCalve.AddCalveView">
        </activity>

        <activity android:name="org.wit.cowcalendar.views.addScan.AddScanView">
        </activity>

    </application>

</manifest>